<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GastosControl">

        <!-- Launcher: Splash -->
        <activity
            android:name=".views.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Resto de pantallas (no son launcher) -->
        <activity android:name=".views.MainActivity" android:exported="false" />
        <activity android:name=".views.CreateActivity" android:exported="false" />
        <activity android:name=".views.DetailActivity" android:exported="false" />
        <activity android:name=".views.EditActivity" android:exported="false" />
    </application>
</manifest>
